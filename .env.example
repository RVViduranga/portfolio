# Optional. Public URL of the deployed site, used for canonical links,
# Open Graph URLs, robots.txt and sitemap.xml. On Vercel it defaults to the
# project's production domain; locally to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
